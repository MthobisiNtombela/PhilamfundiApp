import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PhilamfundiAIPage } from './philamfundi-ai.page';

describe('PhilamfundiAIPage', () => {
  let component: PhilamfundiAIPage;
  let fixture: ComponentFixture<PhilamfundiAIPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PhilamfundiAIPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
